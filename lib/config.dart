// lib/config.dart
// Configuración pública para la app (no incluyas secretos en el repo).
// Para producción se recomienda usar un backend o --dart-define.

const String cloudinaryCloudName = 'df963uwem';
const String cloudinaryUploadPreset = 'diseno_unico_preset';
